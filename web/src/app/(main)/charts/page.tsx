import type { Metadata } from 'next';
import { ComingSoon } from '@/components/ui/ComingSoon';

export const metadata: Metadata = { title: 'Charts' };

export default function ChartsPage() {
  return <ComingSoon page="Charts" />;
}
